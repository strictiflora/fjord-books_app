<%= form_for(current_user.following_users.find_by(followed_id: user.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit t('views.follow.unfollow'), class: "btn btn-default" %>
<% end %>
